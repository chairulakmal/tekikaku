# Shopify app client id (from `shopify app config link`); VITE_ prefix exposes it to index.html.
SHOPIFY_API_KEY=
VITE_SHOPIFY_API_KEY=

# Local Postgres 18 via Docker (compose.yaml); production uses Neon.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/tekikaku
